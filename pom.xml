<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.3.3</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>es.jortri</groupId>
	<artifactId>generadores</artifactId>
	<version>1.5.8</version>
	<name>generadores</name>	
	<description>API rest de generadors con SpringBoot</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>17</java.version>
	</properties>
	<repositories>
		<!-- Incluimos repositorio jitpack para el uso de funcionalidades E-Dumdum -->
	    <repository>
	        <id>jitpack.io</id>
	        <url>https://jitpack.io</url>
	    </repository>
	</repositories>	
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		
		<!--Para usa RandomStringGenerator -->
		<dependency>
		    <groupId>org.apache.commons</groupId>
		    <artifactId>commons-text</artifactId>
		    <version>1.10.0</version>
		</dependency>		
		
		<!-- Para uso del IBANVAlidator de apache commons -->
		<dependency>
		    <groupId>commons-validator</groupId>
		    <artifactId>commons-validator</artifactId>
		    <version>1.7</version>
		</dependency>
		
		<!--Securing REST APIs with API Keys -> https://www.baeldung.com/spring-boot-api-key-secret -->
		<dependency>
		    <groupId>org.springframework.boot</groupId>
		    <artifactId>spring-boot-starter-security</artifactId>
		</dependency>		
		
		<!-- Utilidad para gestionar vin (numeros de bastidor) -->
		<!--
		<dependency>
		    <groupId>com.github.mkyrychenko</groupId>
		    <artifactId>vin-utils</artifactId>
		    <version>1.1.0</version>
		</dependency>
		-->		
		
		<!-- Generar codigos de barras y qr -->
		<dependency>
		    <groupId>com.google.zxing</groupId>
		    <artifactId>core</artifactId>
		    <version>3.3.0</version>
		</dependency>
		<dependency>
		    <groupId>com.google.zxing</groupId>
		    <artifactId>javase</artifactId>
		    <version>3.3.0</version>
		</dependency>		
		
		<!-- Para utilizar funcionadlaida LEI (repositorio jitpack)
			https://github.com/EDumdum/iso-17442-java 
		-->
	    <dependency>
	        <groupId>com.github.EDumdum</groupId>
	        <artifactId>iso-17442-java</artifactId>
	        <version>1.0.0</version>
	    </dependency>
		
	</dependencies>

	<build>
		<!--https://stackoverflow.com/questions/59807912/how-to-compile-spring-boot-maven-plugin-without-version-number
		 Vamos a evitar que el jar se genere como "generadores-1.2.1.jar" con la version, lo que nos obliga a 
		 modificar el dockerfile cada vez que cambiamos la version.
		 De esta forma se genera siempre solo con el nombre de finalName: generadores.jar
		 -->
		<finalName>generadores</finalName>
		<!-- Esto provooca que falle el despliegue con el docker en RENDER... de todas formas
		 la inyeccion de la propiedad version en el application.properties y uso en el controlller
		 parece funcionar sin esto
		-->
		<!--
		<resources>
			<resource>
                <filtering>true</filtering>
				<directory>src/main/resources</directory>
				<includes>
					<include>application.properties</include>
				</includes>
			</resource>
		</resources>
		-->		
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
